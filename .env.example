# Database Configuration
DB_ROOT_PASSWORD=changeme_root_password
DB_NAME=wikijs
DB_USER=wikijs
DB_PASSWORD=changeme_db_password

# Application Ports
WIKIJS_PORT=3000
FILEBROWSER_PORT=8080

# Filebrowser Settings
# Set to true to disable authentication (not recommended for production)
FB_NOAUTH=false
FB_ADMIN_USER=admin
FB_ADMIN_PASSWORD=changeme_filebrowser_admin_password

# Log Rotation Settings
# Maximum size of log file before rotation (e.g., 10m, 100m, 1g)
LOG_MAX_SIZE=10m
# Number of log files to keep
LOG_MAX_FILE=3
