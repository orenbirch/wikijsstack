# Image Configuration (pin to exact tags or digests for production)
MARIADB_IMAGE=mariadb:11
WIKIJS_IMAGE=ghcr.io/requarks/wiki:2
FILEBROWSER_IMAGE=filebrowser/filebrowser:v2

# Database Configuration
DB_ROOT_PASSWORD=
DB_NAME=wikijs
DB_USER=wikijs
DB_PASSWORD=

# Application Ports
WIKIJS_PORT=3000
FILEBROWSER_PORT=8080

# Filebrowser Settings
# Set to true to disable authentication (not recommended for production)
FB_NOAUTH=false
FB_ADMIN_USER=admin
FB_ADMIN_PASSWORD=

# Log Rotation Settings
# Maximum size of log file before rotation (e.g., 10m, 100m, 1g)
LOG_MAX_SIZE=10m
# Number of log files to keep
LOG_MAX_FILE=3
